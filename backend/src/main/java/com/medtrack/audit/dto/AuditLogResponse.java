package com.medtrack.audit.dto; import com.medtrack.inventory.entity.AuditLog; import java.time.Instant; import java.util.*; public record AuditLogResponse(UUID id,UUID userId,String action,String entityName,UUID entityId,String changesJson,Instant createdAt){public static AuditLogResponse of(AuditLog l){return new AuditLogResponse(l.getId(),l.getUserId(),l.getAction(),l.getEntityName(),l.getEntityId(),l.getChangesJson(),l.getCreatedAt());}}
