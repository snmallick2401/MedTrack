package com.medtrack.warehouse.repository; import com.medtrack.warehouse.entity.StorageLocation; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface StorageLocationRepository extends JpaRepository<StorageLocation,UUID>{boolean existsByWarehouse_IdAndBinCode(UUID warehouseId,String binCode); List<StorageLocation> findByWarehouse_Id(UUID warehouseId);}
