package com.medtrack.inventory.repository; import com.medtrack.inventory.entity.IdempotencyKey; import java.util.*; import org.springframework.data.jpa.repository.*; import org.springframework.data.repository.query.Param; public interface IdempotencyKeyRepository extends JpaRepository<IdempotencyKey,UUID>{Optional<IdempotencyKey> findByKeyAndUserIdAndRequestPath(String key,UUID userId,String path); @Query(value="select pg_advisory_xact_lock(hashtextextended(:lockKey, 0))",nativeQuery=true) void lock(@Param("lockKey") String lockKey);}
