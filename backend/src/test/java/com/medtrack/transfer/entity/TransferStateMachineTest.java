package com.medtrack.transfer.entity; import static org.junit.jupiter.api.Assertions.*; import com.medtrack.shared.exception.ConflictException; import org.junit.jupiter.api.Test; class TransferStateMachineTest {@Test void rejectsStateJump(){assertThrows(ConflictException.class,()->TransferStateMachine.assertAllowed(TransferStatus.REQUESTED,TransferStatus.DISPATCHED));}@Test void permitsCanonicalStep(){assertDoesNotThrow(()->TransferStateMachine.assertAllowed(TransferStatus.PACKED,TransferStatus.DISPATCHED));}}
