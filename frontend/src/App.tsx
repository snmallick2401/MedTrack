import{Navigate,Route,Routes}from"react-router-dom";
import{useUiStore}from"./store/uiStore";import{LoginPage}from"./features/auth/LoginPage";import{AppShell}from"./components/layout/AppShell";import{DashboardPage}from"./features/dashboard/DashboardPage";import{InventoryPage}from"./features/inventory/InventoryPage";import{InboundReceivingPage}from"./features/inventory/InboundReceivingPage";import{TrackingPage}from"./features/tracking/TrackingPage";import{ReportsPage}from"./features/reports/ReportsPage";import{BatchesPage,MedicinesPage,SuppliersPage,WarehousesPage}from"./features/master-data/MasterDataPages";import{PlaceholderPage}from"./features/shared/PlaceholderPage";
function Guard(){return useUiStore(s=>s.user)?<AppShell/>:<Navigate to="/login" replace/>}
export default function App(){return <Routes><Route path="/login" element={<LoginPage/>}/><Route path="/app" element={<Guard/>}><Route path="dashboard" element={<DashboardPage/>}/><Route path="inventory" element={<InventoryPage/>}/><Route path="inventory/inbound" element={<InboundReceivingPage/>}/><Route path="medicines" element={<MedicinesPage/>}/><Route path="batches" element={<BatchesPage/>}/><Route path="suppliers" element={<SuppliersPage/>}/><Route path="warehouses" element={<WarehousesPage/>}/><Route path="tracking" element={<TrackingPage/>}/><Route path="reports" element={<ReportsPage/>}/>{['transfers','shipments','audit','labels','notifications'].map(path=><Route key={path} path={path} element={<PlaceholderPage title={path[0].toUpperCase()+path.slice(1)}/>}/>)}</Route><Route path="*" element={<Navigate to="/app/dashboard" replace/>}/></Routes>}
