import{useUiStore}from"../../store/uiStore";export function DashboardPage(){const user=useUiStore(s=>s.user);return <section><h1 className="text-2xl font-semibold text-ink">Operations dashboard</h1><p className="mt-2 text-muted">Welcome, {user?.fullName??"operator"}. Select an operational area from the sidebar.</p><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-4">{['Inventory','Transfers','Shipments','Expiry risk'].map(x=><article key={x} className="rounded-lg border border-border p-6"><h2 className="font-semibold text-ink">{x}</h2><p className="mt-2 text-muted">Live backend data is available from its operational screen.</p></article>)}</div></section>}
