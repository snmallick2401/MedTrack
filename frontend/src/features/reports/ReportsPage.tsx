import{reportApi}from"../../services/reportApi";export function ReportsPage(){async function download(){const blob=await reportApi.inventory();const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='inventory.csv';a.click();URL.revokeObjectURL(url)}return <section><h1 className="text-2xl font-semibold text-ink">Reports</h1><p className="my-4 text-muted">Export the server-authoritative inventory report.</p><button onClick={download} className="h-9 rounded bg-primary px-4 text-white">Export inventory CSV</button></section>}
