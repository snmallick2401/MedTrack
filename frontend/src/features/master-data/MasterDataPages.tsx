import {FormEvent, useState} from "react";
import {useMutation, useQuery, useQueryClient} from "@tanstack/react-query";
import {Plus, Save, X} from "lucide-react";
import {masterDataApi} from "../../services/masterDataApi";
import {ErrorBanner, Empty, Loading} from "../../components/feedback/States";
import {StatusBadge} from "../../components/ui/StatusBadge";
import {errorMessage} from "../../utils/errors";
import type {Batch, Medicine, Supplier, Warehouse} from "../../types/api";

function PageTitle({title,action}:{title:string;action?:React.ReactNode}){return <div className="mb-6 flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-2xl font-semibold text-ink">{title}</h1><p className="text-muted">Authoritative master data used by inventory and fulfillment.</p></div>{action}</div>}
function Button({children,...props}:React.ButtonHTMLAttributes<HTMLButtonElement>){return <button {...props} className={`inline-flex min-h-10 items-center justify-center gap-2 rounded bg-ink px-4 text-sm font-medium text-white disabled:cursor-not-allowed disabled:opacity-50 ${props.className??""}`}>{children}</button>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="grid gap-1 text-sm font-medium text-ink"><span>{label}</span>{children}</label>}
function Panel({children}:{children:React.ReactNode}){return <div className="rounded-lg border border-border bg-canvas p-4 shadow-sm">{children}</div>}
function FormError({error}:{error:unknown}){return error?<ErrorBanner message={errorMessage(error)}/>:null}

export function MedicinesPage(){const [search,setSearch]=useState("");const [show,setShow]=useState(false);const q=useQuery({queryKey:["medicines",search],queryFn:()=>masterDataApi.medicines(0,search)});return <section><PageTitle title="Medicines" action={<Button onClick={()=>setShow(v=>!v)}><Plus size={16}/>New medicine</Button>}/>{show&&<MedicineForm onComplete={()=>{setShow(false);q.refetch()}}/>}<div className="mb-3"><label className="sr-only" htmlFor="medicine-search">Search medicines</label><input id="medicine-search" value={search} onChange={e=>setSearch(e.target.value)} className="w-full max-w-md rounded border border-border px-3 py-2" placeholder="Search by SKU or name"/></div>{q.isLoading?<Loading/>:q.isError?<ErrorBanner message={errorMessage(q.error)}/>:!q.data?.content.length?<Empty title="No medicines found"/>:<Panel><table className="w-full text-left"><thead className="text-xs uppercase text-muted"><tr><th className="py-2">SKU</th><th>Medicine</th><th>Form / strength</th><th>Category</th><th>Status</th></tr></thead><tbody>{q.data.content.map(m=><tr key={m.id} className="border-t border-border"><td className="py-3 font-mono text-xs">{m.sku}</td><td><strong>{m.genericName}</strong><br/><span className="text-muted">{m.brandName}</span></td><td>{m.dosageForm} · {m.strength}</td><td>{m.categoryCode}</td><td><StatusBadge status={m.status}/></td></tr>)}</tbody></table></Panel>}</section>}
function MedicineForm({onComplete}:{onComplete:()=>void}){const [error,setError]=useState<unknown>();const mutation=useMutation({mutationFn:masterDataApi.createMedicine,onSuccess:onComplete,onError:setError});function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);mutation.mutate({sku:String(d.get("sku")),genericName:String(d.get("genericName")),brandName:String(d.get("brandName"))||undefined,categoryId:String(d.get("categoryId")),dosageForm:String(d.get("dosageForm")),strength:String(d.get("strength")),unitOfMeasure:String(d.get("unitOfMeasure")),storageTemp:String(d.get("storageTemp"))||undefined,minStockThreshold:Number(d.get("minStockThreshold")||0),minReceivingShelfLifeDays:Number(d.get("minReceivingShelfLifeDays")||90),status:String(d.get("status")),description:String(d.get("description"))||undefined});}return <Panel><form onSubmit={submit} className="grid gap-3 md:grid-cols-3"><Field label="SKU"><input required pattern="MED-[A-Z]{2,12}-[0-9]{5}" name="sku" placeholder="MED-ANT-00042" className="input"/></Field><Field label="Generic name"><input required name="genericName" className="input"/></Field><Field label="Brand name"><input name="brandName" className="input"/></Field><Field label="Category ID"><input required name="categoryId" className="input" placeholder="Category UUID"/></Field><Field label="Dosage form"><input required name="dosageForm" className="input"/></Field><Field label="Strength"><input required name="strength" className="input"/></Field><Field label="Unit"><input required name="unitOfMeasure" defaultValue="TABLET" className="input"/></Field><Field label="Storage temperature"><input name="storageTemp" className="input" placeholder="15–25°C"/></Field><Field label="Minimum stock"><input name="minStockThreshold" type="number" min="0" defaultValue="0" className="input"/></Field><Field label="Minimum shelf life (days)"><input name="minReceivingShelfLifeDays" type="number" min="0" defaultValue="90" className="input"/></Field><Field label="Status"><select name="status" defaultValue="ACTIVE" className="input"><option>ACTIVE</option><option>DISCONTINUED</option></select></Field><Field label="Description"><input name="description" className="input"/></Field><div className="md:col-span-3"><FormError error={error}/><Button type="submit" disabled={mutation.isPending}><Save size={16}/>{mutation.isPending?"Saving…":"Create medicine"}</Button></div></form></Panel>}

export function BatchesPage(){const [show,setShow]=useState(false);const q=useQuery({queryKey:["batches"],queryFn:()=>masterDataApi.batches()});return <section><PageTitle title="Batches" action={<Button onClick={()=>setShow(v=>!v)}><Plus size={16}/>New batch</Button>}/>{show&&<BatchForm onComplete={()=>{setShow(false);q.refetch()}}/>}{q.isLoading?<Loading/>:q.isError?<ErrorBanner message={errorMessage(q.error)}/>:!q.data?.content.length?<Empty title="No batches found"/>:<Panel><table className="w-full text-left"><thead className="text-xs uppercase text-muted"><tr><th className="py-2">Batch</th><th>Expiry</th><th>Initial qty.</th><th>Status</th></tr></thead><tbody>{q.data.content.map(b=><tr key={b.id} className="border-t border-border"><td className="py-3 font-mono">{b.batchNumber}</td><td>{b.expiryDate}</td><td>{b.initialQuantity}</td><td><StatusBadge status={b.status}/></td></tr>)}</tbody></table></Panel>}</section>}
function BatchForm({onComplete}:{onComplete:()=>void}){const [error,setError]=useState<unknown>();const m=useMutation({mutationFn:masterDataApi.createBatch,onSuccess:onComplete,onError:setError});function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);m.mutate({batchNumber:String(d.get("batchNumber")),medicineId:String(d.get("medicineId")),supplierId:String(d.get("supplierId")),manufacturingDate:String(d.get("manufacturingDate")),expiryDate:String(d.get("expiryDate")),initialQuantity:Number(d.get("initialQuantity")),status:String(d.get("status"))});}return <Panel><form onSubmit={submit} className="grid gap-3 md:grid-cols-3"><Field label="Batch number"><input required name="batchNumber" className="input"/></Field><Field label="Medicine ID"><input required name="medicineId" className="input"/></Field><Field label="Supplier ID"><input required name="supplierId" className="input"/></Field><Field label="Manufactured"><input required type="date" name="manufacturingDate" className="input"/></Field><Field label="Expires"><input required type="date" name="expiryDate" className="input"/></Field><Field label="Initial quantity"><input required min="1" type="number" name="initialQuantity" className="input"/></Field><Field label="Status"><select name="status" defaultValue="ACTIVE" className="input"><option>ACTIVE</option><option>QUARANTINED</option></select></Field><div className="md:col-span-3"><FormError error={error}/><Button disabled={m.isPending}><Save size={16}/>Create batch</Button></div></form></Panel>}

export function WarehousesPage(){const [show,setShow]=useState(false);const [selected,setSelected]=useState<Warehouse>();const q=useQuery({queryKey:["warehouses"],queryFn:()=>masterDataApi.warehouses()});return <section><PageTitle title="Warehouses" action={<Button onClick={()=>setShow(v=>!v)}><Plus size={16}/>New warehouse</Button>}/>{show&&<WarehouseForm onComplete={()=>{setShow(false);q.refetch()}}/>}{q.isLoading?<Loading/>:q.isError?<ErrorBanner message={errorMessage(q.error)}/>:!q.data?.content.length?<Empty title="No warehouses found"/>:<Panel><table className="w-full text-left"><thead className="text-xs uppercase text-muted"><tr><th className="py-2">Code</th><th>Name</th><th>Type</th><th>Status</th><th></th></tr></thead><tbody>{q.data.content.map(w=><tr key={w.id} className="border-t border-border"><td className="py-3 font-mono">{w.code}</td><td>{w.name}<br/><span className="text-xs text-muted">{w.address}</span></td><td>{w.type.replaceAll("_"," ")}</td><td><StatusBadge status={w.status}/></td><td><button onClick={()=>setSelected(w)} className="text-sm text-blue">Storage locations</button></td></tr>)}</tbody></table></Panel>}{selected&&<StorageLocations warehouse={selected} close={()=>setSelected(undefined)}/>}</section>}
function WarehouseForm({onComplete}:{onComplete:()=>void}){const [error,setError]=useState<unknown>();const m=useMutation({mutationFn:masterDataApi.createWarehouse,onSuccess:onComplete,onError:setError});function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);m.mutate({code:String(d.get("code")),name:String(d.get("name")),type:String(d.get("type")),address:String(d.get("address")),contactPhone:String(d.get("contactPhone"))||undefined,status:String(d.get("status"))});}return <Panel><form onSubmit={submit} className="grid gap-3 md:grid-cols-3"><Field label="Code"><input required name="code" className="input"/></Field><Field label="Name"><input required name="name" className="input"/></Field><Field label="Type"><select name="type" className="input"><option>CENTRAL_WAREHOUSE</option><option>DISTRIBUTION_STORE</option></select></Field><Field label="Address"><input required name="address" className="input"/></Field><Field label="Phone"><input name="contactPhone" className="input"/></Field><Field label="Status"><select name="status" className="input"><option>ACTIVE</option><option>INACTIVE</option></select></Field><div className="md:col-span-3"><FormError error={error}/><Button disabled={m.isPending}><Save size={16}/>Create warehouse</Button></div></form></Panel>}
function StorageLocations({warehouse,close}:{warehouse:Warehouse;close:()=>void}){const q=useQuery({queryKey:["storage",warehouse.id],queryFn:()=>masterDataApi.storageLocations(warehouse.id)});const [error,setError]=useState<unknown>();const qc=useQueryClient();const m=useMutation({mutationFn:(body:{zone:string;rack:string;shelf:string;binCode:string})=>masterDataApi.createStorageLocation(warehouse.id,body),onSuccess:()=>qc.invalidateQueries({queryKey:["storage",warehouse.id]}),onError:setError});function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);m.mutate({zone:String(d.get("zone")),rack:String(d.get("rack")),shelf:String(d.get("shelf")),binCode:String(d.get("binCode"))});e.currentTarget.reset();}return <Panel><div className="mb-3 flex justify-between"><h2 className="font-semibold">Storage locations · {warehouse.name}</h2><button onClick={close} aria-label="Close storage locations"><X size={18}/></button></div>{q.isLoading?<Loading/>:<ul className="mb-4 grid gap-2 sm:grid-cols-2">{q.data?.map(s=><li key={s.id} className="rounded bg-surface-soft p-2 font-mono text-sm">{s.binCode}</li>)}</ul>}<form onSubmit={submit} className="grid gap-2 md:grid-cols-5"><input required className="input" name="zone" placeholder="Zone"/><input required className="input" name="rack" placeholder="Rack"/><input required className="input" name="shelf" placeholder="Shelf"/><input required className="input" name="binCode" placeholder="Bin code"/><Button disabled={m.isPending}><Plus size={16}/>Add</Button></form><FormError error={error}/></Panel>}

export function SuppliersPage(){const [show,setShow]=useState(false);const q=useQuery({queryKey:["suppliers"],queryFn:()=>masterDataApi.suppliers()});return <section><PageTitle title="Suppliers" action={<Button onClick={()=>setShow(v=>!v)}><Plus size={16}/>New supplier</Button>}/>{show&&<SupplierForm onComplete={()=>{setShow(false);q.refetch()}}/>}{q.isLoading?<Loading/>:q.isError?<ErrorBanner message={errorMessage(q.error)}/>:!q.data?.content.length?<Empty title="No suppliers found"/>:<Panel><table className="w-full text-left"><thead className="text-xs uppercase text-muted"><tr><th className="py-2">Code</th><th>Name</th><th>Contact</th></tr></thead><tbody>{q.data.content.map(s=><tr key={s.id} className="border-t border-border"><td className="py-3 font-mono">{s.code}</td><td>{s.name}</td><td>{s.contactEmail??s.contactPhone??"—"}</td></tr>)}</tbody></table></Panel>}</section>}
function SupplierForm({onComplete}:{onComplete:()=>void}){const [error,setError]=useState<unknown>();const m=useMutation({mutationFn:masterDataApi.createSupplier,onSuccess:onComplete,onError:setError});function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);m.mutate({name:String(d.get("name")),code:String(d.get("code")),contactEmail:String(d.get("contactEmail"))||undefined,contactPhone:String(d.get("contactPhone"))||undefined,address:String(d.get("address"))||undefined});}return <Panel><form onSubmit={submit} className="grid gap-3 md:grid-cols-3"><Field label="Name"><input required name="name" className="input"/></Field><Field label="Code"><input required name="code" className="input"/></Field><Field label="Email"><input type="email" name="contactEmail" className="input"/></Field><Field label="Phone"><input name="contactPhone" className="input"/></Field><Field label="Address"><input name="address" className="input"/></Field><div className="md:col-span-3"><FormError error={error}/><Button disabled={m.isPending}><Save size={16}/>Create supplier</Button></div></form></Panel>}
