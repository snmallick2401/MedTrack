import{useState}from"react";import{useNavigate}from"react-router-dom";import{authApi}from"../../services/authApi";import{useUiStore}from"../../store/uiStore";import{errorMessage}from"../../utils/errors";export function LoginPage(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const navigate=useNavigate();const setSession=useUiStore(s=>s.setSession);async function submit(e:React.FormEvent){e.preventDefault();try{const r=await authApi.login(email,password);setSession(r.user,r.accessToken);navigate("/app/dashboard")}catch(e){setError(errorMessage(e))}}return <main className="grid min-h-screen place-items-center bg-surface-soft p-4"><form onSubmit={submit} className="w-full max-w-sm rounded-lg border border-border bg-canvas p-6 shadow-sm"><h1 className="mb-6 text-2xl font-semibold text-ink">MedTrack</h1>{error&&<p role="alert" className="mb-3 text-danger">{error}</p>}<label className="mb-3 block">Email<input required type="email" className="mt-1 h-9 w-full rounded border border-border px-3" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="mb-5 block">Password<input required type="password" className="mt-1 h-9 w-full rounded border border-border px-3" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="h-9 w-full rounded bg-primary text-white">Sign in</button></form></main>}
